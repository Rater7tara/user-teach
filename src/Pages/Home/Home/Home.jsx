import React from 'react';
import UserCards from '../../UserCards/UserCards';

const Home = () => {
    return (
        <div>
            <UserCards></UserCards>
        </div>
    );
};

export default Home;